import React from 'react'
import ReactDOM from 'react-dom/client'
import CongelaFit from './CongelaFit'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CongelaFit />
  </React.StrictMode>
)